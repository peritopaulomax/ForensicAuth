"""Deprecated — use scripts/seed_users.py instead.

The sole administrator is provisioned by seed_users.py (paulo.pmgir).
"""

import sys

if __name__ == "__main__":
    print("Use: python scripts/seed_users.py")
    print("Usuario unico: paulo.pmgir (admin, primeiro acesso)")
    sys.exit(0)
