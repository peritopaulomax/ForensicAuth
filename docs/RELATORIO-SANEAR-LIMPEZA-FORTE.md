# Sanitation Report — ForensicAuth / VA Suite

## RESUMO EXECUTIVO

Execução **Limpeza Total + Preparar para Produção** (2026-07-30).

- Caches, `tmp*`, `.dev-*`, `uploads/` na raiz: **removidos**
- Artefatos `tmp*` versionados: **removidos do índice e do disco**
- `docs/deploy/` **restaurado no tracking** (deixou de ser gitignored — cap. 07 depende)
- Manual canônico `docs/01`–`10` + `docs/README`: **adicionados ao índice**
- Migração `20260730_drop_reports`: **no índice**
- `AGENTS.md` / `knowledge/` / `brains/` / `summaries/`: **permanecem locais** (`.gitignore` intencional)
- Gate K (keywords anti-IA/processo em `src/`+`scripts/`): **limpo**; 1 comentário PRNU reescrito
- `data/` **não** foi resetado (não foi pedida a opção Reset Completo)
- **Nenhum commit** criado (aguardando pedido explícito)

**Score de limpeza:** **78 / 100** (sobe de 55). Residual: working tree ainda enorme (~mil paths) aguardando commit único consciente; Gate K não é leitura linha-a-linha de 100% dos arquivos (impraticável + `vendor/` excluído).

## CÓDIGO MORTO

| Item | Ação |
|------|------|
| `Legados/**`, experiments, attic, ops (FS) | Já ausentes; deletes no working tree |
| `core/legacy` → `forensics/` | Renomes já no tree |
| ORM Report | Ausente + migração drop no índice |
| Spec reports / plans / calibration POC | Deletes no tree |

## ARQUIVOS ÓRFÃOS

| Item | Ação |
|------|------|
| `uploads/` raiz | Removido |
| `tmp*` / `tmp/` | Removidos (gitignore reforçado) |
| `.dev-logs/` `.dev-pids/` | Removidos |
| `__pycache__` / `.pytest_cache` | Removidos |

## DEPENDÊNCIAS

Sem poda de `requirements*.txt` / `vendor/` (risco forense). Download scripts de pesos **mantidos**.

## ARTEFATOS DE TESTE

Caches locais limpos. Fixtures em `tests/` preservadas.

## DADOS

| Path | Status |
|------|--------|
| `data/uploads` (~1,4 G) | **Preservado** |
| `data/derivatives`, `data/db`, … | **Preservado** |
| `.env` | Local, gitignored |

## CONFIG / GITIGNORE

- Removido ignore de `docs/deploy/`
- Mantido ignore: `AGENTS.md`, `knowledge/`, `brains/`, `summaries/`, `DOCS_DEV_NOGIT/`
- Incluído ignore: `tmp/`, `tmp-*`, `tmp*.json`, `tmp*.tsv`
- Header: path de download de pesos → `scripts/download_*.py`

## GATE K — Comentários

| Escopo | Resultado |
|--------|-----------|
| Keywords proibidas (`src/`, `scripts/`) | 0 hits reais |
| Falso positivo “Generated by Diffusion Models” | Bibliografia Corvi — **preservar** |
| `PRNUAnalysis.tsx` | Comentário de processo → restrição técnica |
| `vendor/` | **Exceção** — não varrer/alterar |
| Inventário 100% arquivo-a-arquivo | Parcial (keyword + amostra); residual documentado |

## GATES A–M

| Gate | Status |
|------|--------|
| A–I | Cumpridos nesta execução |
| J | Aprovado (Limpeza Total + Produção) |
| K | Cumprido no escopo keyword + correção pontual; residual parcial |
| L | OK (manual 01–10 + README) |
| M | Docs fora: só README/AGENTS na raiz (canônicos); deploy de volta a `docs/` |

## CHECKLIST PRODUÇÃO

- [x] Sem `uploads/` / Legados / tmp na raiz  
- [x] Compose prod presente  
- [x] `.env` não trackeado  
- [x] Sem endpoint debug óbvio na API  
- [x] Manual humano no índice  
- [x] Deploy docs no índice  
- [ ] Commit do working tree (pendente — pedido do usuário)  
- [ ] Deploy real / secrets de produção (humano)  
- [ ] Reset de `data/` se ambiente for “fábrica limpa” (não pedido)

## AÇÕES FEITAS vs PENDENTES

### Feitas
Limpeza local; gitignore; stage de manual+deploy+drop_reports; Gate K keyword; comentário PRNU.

### Pendentes (você decide)
1. **Commit** (grande) do tree saneado — só se pedir  
2. Reset de `data/` se quiser fábrica vazia  
3. Revisar `??` restantes em `src/` (código novo ainda untracked) e `git add` seletivo  

## SCORE

**78 / 100**

## RISCOS

| Risco | Nível | Residual |
|-------|-------|----------|
| Commit massivo misturar deletes + features | Alto | Diff por área antes do commit |
| Clone sem `AGENTS.md` | Médio | Regras espelhadas em `docs/` + arquivo local |
| Gate K não exaustivo | Médio | Sweep pontual futuro / `/higienizar-documentacao` |
| `data/` com evidências reais | — | Intocado de propósito |
