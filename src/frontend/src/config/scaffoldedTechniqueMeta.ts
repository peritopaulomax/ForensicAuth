/** Metadados de técnicas geradas pelo scaffold. Não edite à mão. */

import type { ForensicTechniqueMeta } from "@/config/forensicTechniqueMeta";

export const SCAFFOLDED_TECHNIQUE_META: Record<string, ForensicTechniqueMeta> = {
  // --- scaffold:meta:start ---
// --- scaffold:meta:end ---
};
