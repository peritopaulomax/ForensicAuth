/**
 * IDs de técnicas scaffolded com entrada em docs/references/papers/imdl
 * (manifest principal + scaffolded_manifest.json).
 * Preenchido por ``scripts/technique/scaffold_technique.py``.
 */

export const SCAFFOLDED_TECHNIQUE_PAPER_IDS: ReadonlySet<string> = new Set([
  // --- scaffold:papers:start ---
  // --- scaffold:papers:end ---
]);
