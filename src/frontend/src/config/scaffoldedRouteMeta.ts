/** Rotas de técnicas geradas pelo scaffold. Não edite à mão. */

export const SCAFFOLDED_ROUTE_META: Record<
  string,
  { technique: string; media: string; title?: string }
> = {
  // --- scaffold:routes:start ---
// --- scaffold:routes:end ---
};
