"""Artifact key→filename pairs for techniques created by the scaffold.

Auto-managed by ``scripts/technique/scaffold_technique.py``. Prefer reusing
keys already listed in ``job_artifacts.ARTIFACT_MAPPINGS`` when possible.
"""

from __future__ import annotations

# --- scaffold:artifact-mappings:start ---
SCAFFOLDED_ARTIFACT_MAPPINGS: list[tuple[str, str]] = []
# --- scaffold:artifact-mappings:end ---
