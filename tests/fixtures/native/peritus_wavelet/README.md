# Peritus wavelet DWT (fixture de regressão)

Snapshot mínimo de `dwt.c` / `dwt.h` do Peritus wavelet noise residue, usado só por
`tests/unit/test_wavelet_noise_residue.py` para equivalência C ↔ Python.

Não é runtime do produto. Runtime: `src/backend/forensics/wavelet_noise_residue/`.
